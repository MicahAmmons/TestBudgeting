@using TestBudgeting.Models;
@model Expense

<h2>Create a New Expense</h2>

@using (Html.BeginForm("InsertExpenseToDatabase", "Expense", "Post"))
{

    <div class="form-group">
        <label asp-for="Budget" class="control-label">Budget</label>
        <input type="text" name="Budget" class="form-control" value="@Model.Budget" />
    </div>

    <div class="form-group">
        <label asp-for="Payee" class="control-label">Payee</label>
        <input type="text" name="Payee" class="form-control" value="@Model.Payee" />
    </div>

    <div class="form-group">
        <label asp-for="Amount" class="control-label">Amount</label>
        <input type="number" step="0.01" name="Amount" class="form-control" value="@Model.Amount" />
    </div>

    <div class="form-group">
        <label asp-for="Month" class="control-label">Month</label>
        <input type="number" name="Month" class="form-control" value="@Model.Month" />
    </div>
    <div class="form-group">
        <label asp-for="Day" class="control-label">Day</label>
        <input type="number" name="Day" class="form-control" value="@Model.Day" />
    </div>

    <div class="form-group">
        <label asp-for="Year" class="control-label">Year</label>
        <input type="number" name="Year" class="form-control" value="@Model.Year" />
    </div>

    <input type="submit" value="Create Expense" class="btn btn-primary" />
}
<div asp-validation-summary="All" class="text-danger"></div>


@*
    Table: expenses
    Columns: Number - Budget    -    Payee   -   Date   -   Amount
            INT     VARCHAR(20)  VARCHAR(30)    DATE    DECIMAL(5,2) *@

@* <a href="/Expense/InsertExpense">Insert</a> *@